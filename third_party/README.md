# third_party/

Vendored upstreams, unmodified, each with its own LICENSE. See `docs/thos/licensing.md`.

- `limine/` — bootloader (git submodule, `v9.x-binary` branch). Required to build the ISO.
- `acpica/` — ACPI CA (Phase 1).
- `mesa/` — RADV Vulkan userspace (Phase 4).
