[package]
name = "tarnod-guard-common"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Gemeinsame #[repr(C)]-Event-Typen zwischen tarnod-guard-ebpf (Kernel) und tarnod-guard (Userspace)"

[features]
default = []
# Aktiviert die aya::Pod-Impl, die nur im Userspace-Loader gebraucht wird.
user = ["dep:aya"]

[dependencies]
aya = { workspace = true, optional = true }
