# userland/

- `musl/` — ported libc for the POSIX personality (Phase 2).
- `ntdll/` — the hand-written NT lower boundary (Phase 3).
- `wine-dlls/` — Wine PE-built `kernel32`/`kernelbase`/`user32` as separate LGPL dynamic libs (Phase 3, pending `docs/thos/licensing.md`).
